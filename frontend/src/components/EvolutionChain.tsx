import React, {useCallback, useState } from 'react';
import * as Evolution from '../types/Evolution';
import * as Pokemon from '../types/Pokemon';



const EvolutionChain = ({
    evolutionData,
    isLoading,
    error
}: Evolution.EvolutionChainsProps) => {
    // 1. 상태처리
    
}